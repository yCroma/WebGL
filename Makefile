up:
	cd commands/dev && bash up.sh

down:
	cd commands/dev && bash down.sh

delete:
	cd commands/dev && bash delete.sh

create:
	cd commands/dev && bash create.sh

dev-nuxt:
	cd commands/dev && bash nuxt.sh

prod-build:
	cd commands/prod && bash build.sh

prod-ps:
	cd commands/prod && bash ps.sh

prod-ps-a:
	cd commands/prod && bash ps-a.sh